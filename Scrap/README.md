p rodar o scrapper:

(linux, usa o git bash ou o wsl):

chmod +x run_scrapper.sh

./run_scrapper.sh

(windows)

.\run_scrapper.bat



se quiser rodar o conversor, depois de rodar o run_scrapper, só acessar o .venv e python .\jpg_txt_converter.py (ele depende da pasta download ser criada)...

ate o momento ele nao salva os textos em um arquivo, só printa depois de analisar as imagens

